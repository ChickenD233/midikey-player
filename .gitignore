# ---------- 构建产物 ----------
**/bin/
**/obj/

# ---------- 本地工具链 / 缓存（.NET SDK、NuGet） ----------
.tools/

# ---------- Cloudflare 中转站的依赖（wrangler 等，用 npm install 装回来） ----------
# 只忽略依赖目录；package.json 与 package-lock.json 要进仓库，
# 否则别人（和 Cloudflare 的构建机）装不出同一份依赖。
node_modules/
# wrangler dev 的本机状态（临时对象库、日志），不进仓库。
.wrangler/

# ---------- 系统文件 ----------
**/.DS_Store

# ---------- 开发自检项目（冒烟测试等，不上传） ----------
MidiKeyPlayerSmoke/

# ---------- 本地发布产物（成品 exe 改走 GitHub Releases） ----------
# release/ 是正式产物目录；release-xxx/ 是临时构建实验目录（比对体积用）。
# 两者都只留在本机，绝不进仓库：一个 exe 有 20 MB 到 45 MB。
release/
release-*/
**/release-*/
